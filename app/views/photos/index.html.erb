<section id="photos">
  <div class="gallery-header">
    <h1><%= "All Photos" %></h1>
    <p><%= pluralize(@pagy.count, "photo") %></p>
  </div>

  <%= render "photos/photo_results", pagy: @pagy, photos: @photos, liked_photo_ids: @liked_photo_ids %>
</section>
